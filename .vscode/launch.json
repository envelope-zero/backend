{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run API (SQLite)",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/main.go",
      "env": {
        "GIN_MODE": "debug"
      },
      "console": "integratedTerminal"
    }
  ]
}
